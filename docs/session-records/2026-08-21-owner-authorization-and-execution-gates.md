# 2026-08-21 — Owner authorization and execution gates

## Record status

This is an operational authorization record for the active strict-successor
goal. It records the Owner's user-authored direction in Codex task
`01a021e2-81d2-7850-a9ee-916fba788e03` at `2026-08-21T01:26:05Z`.

It is not a cryptographic signature, a legal opinion, a data-custody receipt,
an independent scientific approval, a cloud-provider receipt, or a signed
`BuildApprovalV1`. Those artifacts remain fail-closed and must be produced by
their required actors against exact bytes, builds, environments, and times.

## Owner-authorized execution scope

The Owner authorizes the project to pursue the complete strict scientific and
production closure described below:

1. process the authorized Class 1 coded-row source for the scientific parity
   oracle, subject to isolated custody, de-identification review, exact-byte
   hashing, frozen mapping, retention, access, and actor-separation controls;
2. have non-implementation reviewers execute and adjudicate the rENA 0.2.7,
   rENA 0.3.1, and TypeScript raw-oracle outputs for all 55 frozen scientific
   quantities;
3. create an upstream jENA successor commit, obtain independent review, and
   publish the reviewed public `jena-js@0.6.3` package;
4. establish an Owner-controlled, provenance-matched Git remote and push the
   reviewed source history;
5. use the Owner's authorized Vercel and Neon account context, plus approved
   Fly and either Vercel Blob or S3/KMS resources, for real migrations,
   multi-Machine execution, restart, capacity, TTL, and deletion probes;
6. implement and verify the `.ena3d.json` service parser, transactional
   activation, product flow, and all six derived-analysis product families,
   including scientific controls, UI, Plotly visualizations, accessible tables,
   and formal exports;
7. execute Firefox, Playwright WebKit, real Safari, VoiceOver, and NVDA
   acceptance and obtain immutable receipts;
8. execute CodeQL, secret scanning, parser fuzzing, Trivy image scanning,
   license/legal review, and DPA/region approval gates; and
9. bind preview, two-hour stress, 24-hour soak, ordered 5/25/100 canary,
   production probes, and rollback drill to the same signed
   `BuildApprovalV1`.

This authorization includes creating and using reversible, least-privilege
staging resources and incurring only the provider budget limits that are
explicitly observed and bound in the corresponding cloud approval record.
Unknown or unbounded billing, credentials, regions, destructive deletion,
public publication, production traffic changes, and access-control mutations
must remain fail-closed until the exact provider/account/build receipt is
available and any action-time confirmation required by the operating surface
has been satisfied.

## What this record closes

- Owner intent and scope are no longer open planning questions.
- Creating a provenance-matched private `HUDongpin/j-3dENA` remote, pushing the
  existing source branches, opening a draft PR, and running remote CI/security
  workflows are authorized.
- Preparing the upstream jENA review branch/PR and the cloud staging plan is
  authorized.
- The intended first-release scope is strict whole-repository semantic
  replacement, not public `/app` parity alone.

## What this record does not close

- It does not identify or attest the Class 1 raw source bytes. The current
  prepared `.ena3d.json` candidate is not raw coded-row evidence.
- It does not replace the four distinct Class 1 custody actors required by
  `3dena.class1-custody-receipt.v1`: data owner, de-identification reviewer,
  custodian, and signing operator.
- It does not approve any of the 55 rows in
  `3dena.scientific-authority-matrix.v1`; each row still requires exact
  three-oracle artifacts, quantity-specific tolerances, regression evidence,
  and a non-implementation reviewer.
- It does not approve or publish `jena-js@0.6.3`.
- It does not supply cloud secrets, billing limits, regions, migrations,
  Machine identities, object-store identities, or deletion receipts.
- It does not satisfy real-browser, screen-reader, CodeQL, secret, fuzz, Trivy,
  legal, DPA, region, preview, stress, soak, canary, production, deletion, or
  rollback receipts.
- It does not sign or activate a `BuildApprovalV1`, and it cannot be used to
  bypass independent reviewer separation.

## Completion language

Until the machine-readable scientific authority matrix, strict capability
ledger, jENA successor gate, and exact-build release receipt gate all pass on
the same reviewed lineage, the whole-product status remains
`IMPLEMENTED_UNVERIFIED`.
